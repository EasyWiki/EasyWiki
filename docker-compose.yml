version: '3'

services:
    easywiki:
      container_name: easywiki
      restart: always
      build: .
      ports: 
        - "80:80"
        - "443:443"
      volumes: 
        - ./logs:/var/easywiki/logs
        - ./config:/var/easywiki/config
        - ./ssl:/var/easywiki/ssl